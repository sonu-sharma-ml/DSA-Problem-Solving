# Explanation

**Difficulty**: `Medium`
**Time Complexity**: `O(N)`
**Space Complexity**: `O(1)`

## Approach
Floyd's algorithm. Deploying two relative iterating referencing pointers moving uniformly across varying speeds natively proves cyclomatic structures strictly if their parameters concurrently overlap within boundaries referencing effectively matching.
