# Explanation

**Difficulty**: `Easy`
**Time Complexity**: `O(1)`
**Space Complexity**: `O(1)`

## Approach
A foundational mapping example strictly hard-coding individual distinct `Node` instantiations in memory and linking them statically utilizing object attributes without a dynamic structural class wrapper.
